<odoo>
    <!--
        The Auditlog User / Manager groups exist only as ACL anchors.
        They are intentionally not bound to a res.groups.privilege so
        they do not appear as a separate dropdown on the Access Rights
        tab — the MCP Pro Administrator group implies group_auditlog_manager.
    -->

    <record model="res.groups" id="group_auditlog_user">
        <field name="name">Auditlog User</field>
    </record>

    <record model="res.groups" id="group_auditlog_manager">
        <field name="name">Auditlog Manager</field>
        <field
            name="implied_ids"
            eval="[Command.link(ref('pan_mcp_auditlog.group_auditlog_user'))]"
        />
    </record>

    <record id="base.group_system" model="res.groups">
        <field
            name="implied_ids"
            eval="[Command.link(ref('pan_mcp_auditlog.group_auditlog_manager'))]"
        />
    </record>
</odoo>
