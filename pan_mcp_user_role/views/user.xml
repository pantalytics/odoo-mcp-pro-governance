<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2014 ABF OSIELL <http://osiell.com>
     License LGPL-3.0 or later (http://www.gnu.org/licenses/lgpl).
 -->
<odoo>
    <!-- MCP Pro (issue #26): the upstream OCA "User Roles" page, which put
         `role_line_ids` (and the helper `role_ids`) on the user form, is
         deliberately NOT rendered by this inherit.

         Assigning a role to a *user* makes `set_groups_from_roles()` replace
         that employee's groups with the role's closure — correct OCA
         behaviour, but not something this product promises. MCP Pro binds a
         role to an API key (`res.users.apikeys.x_role_id`), which reads
         `role.group_id | role.implied_ids` and never needs the role to be
         assigned to the user.

         The model fields are untouched on purpose: pre-existing role lines
         keep working, and the "managed by roles" alert below still explains
         them. Do not re-add that page when re-vendoring OCA base_user_role. -->
    <record id="view_res_users_form_inherit" model="ir.ui.view">
        <field name="name">res.users.form.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='access_rights']/group" position="before">
                <field name="show_alert" invisible="1" />
                <div
                    class="alert alert-info text-center o_form_header"
                    invisible="not show_alert"
                    role="alert"
                >
                    <div>
                        <strong
                        >The access rights of this user are managed by roles.</strong>
                    </div>
                    <div>
                        <em
                        >Any configuration changes made here will not be persistent.</em>
                    </div>
                    <a class="close" data-bs-dismiss="alert" href="#">x</a>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_res_users_search_inherit" model="ir.ui.view">
        <field name="name">res.users.search.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_search" />
        <field name="arch" type="xml">
            <field name="company_ids" position="after">
                <field
                    name="role_ids"
                    filter_domain="[('role_line_ids.role_id.name','ilike',self)]"
                />
            </field>
        </field>
    </record>
    <record id="view_res_users_tree_inherit" model="ir.ui.view">
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_tree" />
        <field name="arch" type="xml">
            <field name="role" position="after">
                <field name="role_ids" widget="many2many_tags" />
            </field>
        </field>
    </record>
</odoo>
